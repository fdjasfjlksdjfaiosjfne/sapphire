{
    "type": "object",
    "properties": {
        "enabled": {
            "type": "boolean",
            "description": "Turn this off if you're feeling masochistic.",
            "default": true
        },
        "enableUnless": {
            "type": "boolean",
            "default": true
        },
        "syntax": {
            "conditonal": {
                "enum": ["if"],
                "default": "if"
            },
            "unless": {

            },
            "fallbackWithCondition": {
                "type": "string",
                "enum": [
                    "elseif",
                    "else if",
                    "elif",
                    "elsif",
                    "perchance",
                    "assuming",
                    "but what about if"
                ],
                "enumDescriptions": [
                    "Use by Ruby",
                    "Use by a majority of C-based languages",
                    "Use by Python",
                    "Use by Perl",
                    "A alternate, formal way",
                    "A alternate, formal way",
                    "Its design is very human"
                ],
                "default": "elif"
            },
            "fallback": {
                "enum": [
                    "else",
                    "otherwise"
                ],
                "default": "else"
            }
        }
    }
}